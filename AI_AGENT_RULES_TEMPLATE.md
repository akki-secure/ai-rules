# AIエージェント向け ルール指示書テンプレート

このドキュメントは、Claude Code / Gemini などのAIコーディングエージェントに開発を任せる際に、
**共通して守らせたいルール**をまとめたテンプレートです。
`{ }` で囲われた箇所はプロジェクトごとに書き換えてください。

対象プロジェクト: `{アプリ名}`
リポジトリ: `{リポジトリURL}`

---

## 1. 要件定義書とプロトタイプ

まず最初に、何を作るかを固める。実装(バックエンド/フロントエンド)より前に行うこと。

1. 要件定義書を作ってもらう(必ず `ask` で確認しながら進める)。
2. 要件定義書ができたら、`curl` コマンドでの動作確認ではなく、
   **ブラウザでHTML/CSS/JavaScriptが動く静的なモック(プロトタイプ)** を作成し、
   ブラウザ操作の様子が見えるようにゆっくり動作を確認する。
3. `README.md` とは別に、**要件定義書専用のMarkdownファイル**(例: `docs/requirements.md`)を作成する。

---

## 2. 開発フロー(Plan Mode)

要件定義書とプロトタイプで合意が取れたら、`{アプリ名}` の実装を以下の順序で進めること。

1. **バックエンドを作る**
2. **フロントエンドを作る**
3. **ガードレールを作る**
   - 確認動作(dry-run・プレビュー)のためであっても、**破壊的操作の実行は禁止**する。
   - 上記と同時に、以下のログ設計を行う。
     - 構造化ログ(JSON形式など、機械可読なログ)
     - 運用ログ設計(誰が・いつ・何をしたかを追跡できる設計)
     - 監視ログ設計(異常検知・アラートに使えるログ設計)
4. **自動テストを作る**
   - アプリの機能を1つ作り終えるたびに、その機能の自動テストも作成・実行する。
   - コードが長くなる場合は、可読性のために**日本語メソッド名**の使用を検討するよう伝える。
5. **パフォーマンステストは自動テストと別枠**とする。
   - 機能追加のたびには実行しない。
   - 任意のタイミングで手動実行する(実行方法は「9. Skills / `.claude/commands/` フォルダの作成」を参照)。

---

## 3. 不明な用語の確認

作業中にわからない専門用語・略語が出てきた場合は、憶測で進めず必ずユーザーに質問すること。

---

## 4. ドキュメントの分離

`README.md` とは別に、Markdown形式のファイルとして保存する(要件定義書、ルール指示書、設計メモなど、目的ごとにファイルを分ける)。

---

## 5. 品質チェックとデータ保護

1. 動作確認の前に、**品質チェック(静的解析)** を必ず行う。
   - 機能単位の正しさ(バグ・型エラー・Lint)だけでは見落とされる問題があるため、
     **リポジトリ全体の棚卸し**(未使用のファイル・フォルダ、重複した実装、
     どこからも参照されていないコードが残っていないか)も定期的に確認する。
   - 特に、途中で作って使わなくなった雛形・試作コード(例: 別フレームワークで
     作りかけたフォルダなど)は、削除するか「未使用」と明記しない限り残さない。
2. **SQLのデータを壊さないこと。**
   - 破壊的操作によってデータが全て消失すると重大インシデントになるため、
     `DELETE` / `DROP` / `TRUNCATE` などは慎重に扱い、必ず事前確認を取る。
3. 自動テストを実行する。

> ⚠️ **注意**: テストの実行が30分以上かかっている場合、フリーズしている可能性が高い。
> その場合は処理を止めて、ユーザーに状況を確認させること。

---

## 6. Gitブランチ運用・`.gitignore`・LICENSEの整備

1. `main` ブランチはユーザー自身が作成する。
2. AIエージェントは **`main` に直接pushしない**。
   - 必ず作業用ブランチ(例: `feature/{機能名}`)を作成し、そこにpushする。
   - `main` へのマージはユーザーが確認・実施する(PR経由を推奨)。
3. **最初のコミットをする前に `.gitignore` を必ず作成する。**
   - 機密情報(APIキー、パスワード、認証情報)をコミットしてしまうと、
     Gitの履歴から完全に消すのは困難で、漏洩リスクが残り続ける。
   - 以下のようなものは基本的にすべて `.gitignore` に含めること。

   | 分類 | 例 | 理由 |
   |---|---|---|
   | 機密情報 | `.env`, `.env.*`, `*.pem`, `*.key`, `credentials.json`, `secrets.*` | APIキー・パスワード等の漏洩防止 |
   | 依存関係・自動生成物 | `node_modules/`, `vendor/`, `.venv/`, `__pycache__/`, `dist/`, `build/` | 再生成できるため、容量が無駄になるだけでなく差分が汚れる |
   | OS・エディタ固有ファイル | `.DS_Store`, `Thumbs.db`, `.idea/`, `.vscode/*`(個人設定除く) | 個人環境依存のファイルで、チームに不要 |
   | ログ・キャッシュ | `*.log`, `logs/`, `coverage/`, `.nyc_output/` | 実行のたびに変わるため、履歴を汚す |
   | Terraformの状態ファイル | `*.tfstate`, `*.tfstate.backup`, `.terraform/` | **stateファイルにはリソースIDやシークレットが平文で含まれることがあるため特に注意**(「14. デプロイ」を参照) |
   | ローカルDB・一時ファイル | `*.sqlite3`, `*.db`, `tmp/` | 開発用の一時データで共有不要 |

4. **`.gitignore` と合わせて `LICENSE` ファイルも最初のコミット時に用意する。**
   - LICENSEがないと、法律上は「著作権者に無断でコピー・改変・商用利用してはいけない」がデフォルトの扱いになる。
   - 特別な理由がなければ `{ライセンス種別(例: MIT)}` を採用し、誰でも自由に再利用できるようにする。
   - ライセンス文中の著作権者名・年は `{著作権者名}` / `{年}` を実際の値に置き換える。

---

## 7. 完了報告のテンプレート

一連の作業が終わったら、以下のように締めくくること。

> 「ありがとう。今回はここまでいいです。お疲れ様でした。」

---

## 8. 作業環境の切り替え

次の段階に進む前に、CursorなどでClaude Codeのセッションを新しく開き直してから進める
(コンテキストをリセットし、次のタスクに集中させるため)。

---

## 9. Skills / `.claude/commands/` フォルダの作成

- `skills`(Claudeのコマンドショートカット)と `.claude/commands/` フォルダを作成し、
  それぞれの用途に応じた専用ファイルを用意する。
- Claudeとのチャットでは `/コマンド名` で指示を出せるようにする。
- パフォーマンステストは `/performance-test` のような専用コマンドを作成し、
  必要なタイミングで手動実行する。

---

## 10. 全体の品質チェック(複数回)

AIが1回のチェックで見逃す不具合もあるため、品質チェックは複数回行う。

- 品質チェックを「特定機能の確認」に限定せず、少なくとも1回は
  **リポジトリ構成全体(フォルダ一覧・README整合性)を俯瞰する回**を含める。
- SQLが複数行にわたって読みづらくなる場合は、**XML形式でSQLを管理する**よう指示する。
- 画像の中身については、実際に画像を見るまで**推測で断定しない**。

---

## 11. 最終確認

すべての修正・実装が終わったら、最後にもう一度自動テストを実行する。

---

## 12. ドキュメントの継続的な更新

GitHubの `README.md` には、以下を必ず追記する。

- 画面遷移図
- モック(UIモックアップ)
- ER図(データベース設計図)

その後、**API仕様書**の仕組み(例: OpenAPI/Swagger)を作成するよう依頼する。

> 機能追加があった場合は、上記(遷移図・モック・ER図・API仕様書)の更新を必ず行うこと。

---

## 13. プロジェクトについてのヒアリング

作成したプロジェクトについて、AIエージェント側からユーザーに質問してもらい、
仕様の認識合わせや不明点の洗い出しを行う。

---

## 14. デプロイ

デプロイを実行する前に、必ずユーザーに確認を取ること。
承認が得られたら、`{クラウドプロバイダ名(例: AWS)}` を使用し、
Terraformによるインフラ構築とデプロイを実行する。

---

## 15. リソースの削除(コスト管理)

デプロイ環境を稼働させたままにするとコストが発生し続けるため、不要になった時点で削除する。
削除の際は、以下の手順を必ず踏むこと。

1. 削除対象のデータのバックアップを取得する。
2. これまで蓄積されたログのバックアップも取得する。
3. バックアップ完了をチャットで報告する。
4. ユーザーの確認を得た上で `terraform destroy` を実行する。

---

## 補足: プレースホルダー一覧

| プレースホルダー | 説明 |
|---|---|
| `{アプリ名}` | 開発するアプリケーションの名称 |
| `{リポジトリURL}` | プロジェクトのGitHubリポジトリURL |
| `{機能名}` | 作業ブランチ名に使う機能の識別名 |
| `{クラウドプロバイダ名(例: AWS)}` | デプロイ先のクラウドプロバイダ |
| `{ライセンス種別(例: MIT)}` | リポジトリに適用するライセンスの種類 |
| `{著作権者名}` | LICENSEファイルに記載する著作権者(個人名・組織名) |
| `{年}` | LICENSEファイルに記載する年 |

---

## 記入例

家計簿アプリ `MyKakeibo` を1人で開発するケースを想定した記入例です。

- `{アプリ名}` → `MyKakeibo`
- `{リポジトリURL}` → `https://github.com/example-user/mykakeibo`
- `{機能名}` → `add-expense-form`(支出入力フォーム機能のブランチ例: `feature/add-expense-form`)
- `{クラウドプロバイダ名(例: AWS)}` → `AWS`
- `{ライセンス種別(例: MIT)}` → `MIT`
- `{著作権者名}` → `example-user`
- `{年}` → `2026`

この場合、ルール6は以下のように読み替えられます。

> 作業用ブランチ `feature/add-expense-form` を作成してpushする。`LICENSE` ファイルはMITライセンスとし、著作権者は `example-user`、年は `2026` とする。
